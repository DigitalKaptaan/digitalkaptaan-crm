import { Breadcrumb } from "@/components";
import { LayoutWrapper } from "@/layout";
import React from "react";

const page = () => {
  return (
    <LayoutWrapper>
      <Breadcrumb />
    </LayoutWrapper>
  );
};

export default page;
